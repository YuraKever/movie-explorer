/**
 * Единая точка сборки схемы БД: таблицы Better Auth (сгенерированы CLI,
 * `auth-schema.ts` — не редактировать руками) и наши таблицы приложения.
 */
export * from "./auth-schema";
export * from "./favorites-schema";
